(ns com.amithgeorge.pos.pos-test
    (:require [clojure.test :refer [deftest testing is]]
              [com.amithgeorge.pos.pos :as sut]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
